import { t as __commonJS } from "./chunk-mMvWCSj1.js";
import { t as require_react } from "./react-D0oT4HgQ.js";
import "./react-dom-DbP1T2p5.js";
import { t as require_client } from "./client-BtSq7VO3.js";

//#region node_modules/sweetalert2-react-content/dist/sweetalert2-react-content.umd.js
var require_sweetalert2_react_content_umd = /* @__PURE__ */ __commonJS({ "node_modules/sweetalert2-react-content/dist/sweetalert2-react-content.umd.js": ((exports, module) => {
	(function(global, factory) {
		typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory(require_react(), require_client()) : typeof define === "function" && define.amd ? define(["react", "react-dom/client"], factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, global.sweetalert2ReactContent = factory(global.React, global.ReactDOM));
	})(exports, (function(React, client) {
		const mounts = [
			{
				key: "title",
				getter: (swal) => swal.getTitle()
			},
			{
				key: "html",
				getter: (swal) => swal.getHtmlContainer()
			},
			{
				key: "confirmButtonText",
				getter: (swal) => swal.getConfirmButton()
			},
			{
				key: "denyButtonText",
				getter: (swal) => swal.getDenyButton()
			},
			{
				key: "cancelButtonText",
				getter: (swal) => swal.getCancelButton()
			},
			{
				key: "footer",
				getter: (swal) => swal.getFooter()
			},
			{
				key: "closeButtonHtml",
				getter: (swal) => swal.getCloseButton()
			},
			{
				key: "iconHtml",
				getter: (swal) => swal.getIconContent()
			},
			{
				key: "loaderHtml",
				getter: (swal) => swal.getLoader()
			}
		];
		const noop = () => {};
		function withReactContent(ParentSwal) {
			function extractReactParams(params) {
				const reactParams = {};
				const otherParams = {};
				const mountKeys = mounts.map((mount) => mount.key);
				Object.entries(params).forEach((_ref) => {
					let [key, value] = _ref;
					if (mountKeys.includes(key) && /* @__PURE__ */ React.isValidElement(value)) {
						reactParams[key] = value;
						otherParams[key] = " ";
					} else otherParams[key] = value;
				});
				return [reactParams, otherParams];
			}
			function render(swal, reactParams) {
				Object.entries(reactParams).forEach((_ref2) => {
					let [key, value] = _ref2;
					const domElement = mounts.find((mount) => mount.key === key).getter(ParentSwal);
					const root = client.createRoot(domElement);
					root.render(value);
					swal.__roots.push(root);
				});
			}
			function unrender(swal) {
				swal.__roots.forEach((root) => {
					root.unmount();
				});
				swal.__roots = [];
			}
			return class extends ParentSwal {
				static argsToParams(args) {
					if (/* @__PURE__ */ React.isValidElement(args[0]) || /* @__PURE__ */ React.isValidElement(args[1])) {
						const params = {};
						[
							"title",
							"html",
							"icon"
						].forEach((name, index) => {
							if (args[index] !== void 0) params[name] = args[index];
						});
						return params;
					} else return ParentSwal.argsToParams(args);
				}
				_main(params, mixinParams) {
					this.__roots = [];
					this.__params = Object.assign({}, mixinParams, params);
					const [reactParams, otherParams] = extractReactParams(this.__params);
					const superWillOpen = otherParams.willOpen || noop;
					const superDidOpen = otherParams.didOpen || noop;
					const superDidDestroy = otherParams.didDestroy || noop;
					return super._main(Object.assign({}, otherParams, {
						willOpen: (popup) => {
							render(this, reactParams);
							superWillOpen(popup);
						},
						didOpen: (popup) => {
							setTimeout(() => {
								superDidOpen(popup);
							});
						},
						didDestroy: (popup) => {
							superDidDestroy(popup);
							unrender(this);
						}
					}));
				}
				update(params) {
					Object.assign(this.__params, params);
					unrender(this);
					const [reactParams, otherParams] = extractReactParams(this.__params);
					super.update(otherParams);
					render(this, reactParams);
				}
			};
		}
		return withReactContent;
	}));
}) });

//#endregion
export default require_sweetalert2_react_content_umd();

//# sourceMappingURL=sweetalert2-react-content.js.map